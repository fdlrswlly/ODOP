<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fadhlyaulia.odop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/odop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/odop"
        android:supportsRtl="true"
        android:theme="@style/Theme.ODOP">
        <activity android:name=".DoaActivity"></activity>
        <activity android:name=".AddActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".LaporanActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MukadimahActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoadActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>